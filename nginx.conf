server {
    listen 80;
    server_name localhost;
    
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://admin-backend:8003;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    location /ddos-api/ {
        rewrite ^/ddos-api/(.*) /$1 break;
        proxy_pass http://ddos-backend:8004;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    location /api/ml/ddos/ {
        rewrite ^/api/ml/ddos/(.*) /$1 break;
        proxy_pass http://ml-ddos:8005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/exploit/ {
        rewrite ^/api/ml/exploit/(.*) /$1 break;
        proxy_pass http://ml-exploit:8006;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/anomaly/ {
        rewrite ^/api/ml/anomaly/(.*) /$1 break;
        proxy_pass http://ml-anomaly:8007;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/isolation/ {
        rewrite ^/api/ml/isolation/(.*) /$1 break;
        proxy_pass http://ml-isolation:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/random/ {
        rewrite ^/api/ml/random/(.*) /$1 break;
        proxy_pass http://ml-random:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/classifier/ {
        rewrite ^/api/ml/classifier/(.*) /$1 break;
        proxy_pass http://ml-classifier:8008;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/nlp/ {
        rewrite ^/api/ml/nlp/(.*) /$1 break;
        proxy_pass http://ml-nlp:8009;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/ip-reputation/ {
        rewrite ^/api/ml/ip-reputation/(.*) /$1 break;
        proxy_pass http://ml-ip-reputation:8010;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/rate-limiter/ {
        rewrite ^/api/ml/rate-limiter/(.*) /$1 break;
        proxy_pass http://ml-rate-limiter:8011;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/behavioral/ {
        rewrite ^/api/ml/behavioral/(.*) /$1 break;
        proxy_pass http://ml-behavioral:8012;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ml/performance/ {
        rewrite ^/api/ml/performance/(.*) /$1 break;
        proxy_pass http://ml-performance:8013;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
